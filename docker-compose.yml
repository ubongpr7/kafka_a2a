x-ka2a-image: &ka2a_image
  # Shared image build config so `docker compose up` can build locally without pulling from a registry.
  # If you publish an image to Docker Hub, you can remove `build:` and set `image:` to your tag.
  image: kafka-a2a:local
  build:
    context: .
    args:
      KA2A_UV_EXTRAS: "server,auth,lang,redis,mcp,voice"
  env_file:
    # Production deploys should export `KA2A_ENV_FILE=.env.prod` so the
    # containers do not start with the local development `.env`.
    - ${KA2A_ENV_FILE:-.env}

services:
  host-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/host_agent.txt
      KA2A_TOOL_EXECUTOR: kafka_a2a.local_tools:build_interaction_tool_executor
      KA2A_ALLOWED_DOWNSTREAM_AGENTS: onboarding,users,product,inventory,pos
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/host.agent-card.json",
        "--processor",
        "langgraph-chat",
        "--push-notifications",
      ]
    restart: unless-stopped


  onboarding-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/onboarding_agent.txt
      KA2A_TOOL_EXECUTOR: kafka_a2a.local_tools:build_interaction_tool_executor
      KA2A_ALLOWED_DOWNSTREAM_AGENTS: users,product,inventory
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/onboarding.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped
  product-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/product_agent.txt
      KA2A_TOOL_EXECUTOR: kafka_a2a.local_tools:build_interaction_tool_executor
      KA2A_ALLOWED_DOWNSTREAM_AGENTS: product_discovery,marketplace_sourcing,product_catalog_admin,product_merchandising,product_pricing
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/product.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  product-discovery-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/product_discovery_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/product_discovery.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  marketplace-sourcing-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/marketplace_sourcing_agent.txt
      KA2A_TOOL_EXECUTOR: kafka_a2a.marketplace_tools:build_marketplace_sourcing_tool_executor
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/marketplace_sourcing.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  product-catalog-admin-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/product_catalog_admin_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/product_catalog_admin.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  product-merchandising-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/product_merchandising_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/product_merchandising.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  product-pricing-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/product_pricing_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/product_pricing.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  inventory-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/inventory_agent.txt
      KA2A_TOOL_EXECUTOR: kafka_a2a.local_tools:build_interaction_tool_executor
      KA2A_ALLOWED_DOWNSTREAM_AGENTS: inventory_visibility,inventory_setup,inventory_procurement,inventory_fulfillment
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/inventory.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped
  inventory-visibility-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/inventory_visibility_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/inventory_visibility.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped


  inventory-setup-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/inventory_setup_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/inventory_setup.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  inventory-procurement-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/inventory_procurement_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/inventory_procurement.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  inventory-fulfillment-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/inventory_fulfillment_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/inventory_fulfillment.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  users-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/users_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/users.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  pos-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/pos_agent.txt
      KA2A_TOOL_EXECUTOR: kafka_a2a.local_tools:build_interaction_tool_executor
      KA2A_ALLOWED_DOWNSTREAM_AGENTS: pos_live,pos_admin
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/pos.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  pos-live-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/pos_live_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/pos_live.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  pos-admin-agent:
    <<: *ka2a_image
    volumes:
      - ./agent_cards:/app/agent_cards:ro
      - ./prompts:/app/prompts:ro
      - ./mcp-tools.prod.json:/app/mcp-tools.prod.json:ro
    environment:
      KA2A_SYSTEM_PROMPT_PATH: /app/prompts/pos_admin_agent.txt
    command:
      [
        "agent",
        "--agent-card-path",
        "/app/agent_cards/pos_admin.agent-card.json",
        "--processor",
        "langgraph-chat"
      ]
    restart: unless-stopped

  gateway:
    <<: *ka2a_image
    depends_on:
      - host-agent
      - onboarding-agent
      - users-agent
      - product-agent
      - product-discovery-agent
      - marketplace-sourcing-agent
      - product-catalog-admin-agent
      - product-merchandising-agent
      - product-pricing-agent
      - inventory-agent
      - inventory-visibility-agent
      - inventory-setup-agent
      - inventory-procurement-agent
      - inventory-fulfillment-agent
      - pos-agent
      - pos-live-agent
      - pos-admin-agent
    environment:
      KA2A_RUNTIME_SHARED_TOKEN: ${KA2A_RUNTIME_SHARED_TOKEN:-interaims-local-ka2a-runtime}
    ports:
      - "7006:7006"
    command: ["gateway", "--default-agent", "host", "--host", "0.0.0.0", "--port", "7006"]
    restart: unless-stopped

  voice-agent:
    <<: *ka2a_image
    depends_on:
      - gateway
      - host-agent
    environment:
      KA2A_RUNTIME_SHARED_TOKEN: ${KA2A_RUNTIME_SHARED_TOKEN:-interaims-local-ka2a-runtime}
      KA2A_CONTROL_PLANE_BASE_URL: http://gateway:7006
      KA2A_LIVEKIT_AGENT_NAME: ka2a-voice
      KA2A_VOICE_HOST_AGENT_NAME: host
      KA2A_VOICE_TTS_VOICE: marin
      KA2A_VOICE_STT_MODEL: whisper-1
      KA2A_VOICE_TTS_MODEL: gpt-4o-mini-tts
      KA2A_VOICE_LLM_MODEL: gpt-4.1-mini
      KA2A_VOICE_HOST_TIMEOUT_S: "240"
      KA2A_VOICE_BACKEND_DELEGATION_ENABLED: "false"
    command: ["voice"]
    restart: unless-stopped

  a2a-proxy:
    <<: *ka2a_image
    depends_on:
      - host-agent
      - onboarding-agent
      - users-agent
      - product-agent
      - product-discovery-agent
      - marketplace-sourcing-agent
      - product-catalog-admin-agent
      - product-merchandising-agent
      - product-pricing-agent
      - inventory-agent
      - inventory-visibility-agent
      - inventory-setup-agent
      - inventory-procurement-agent
      - inventory-fulfillment-agent
      - pos-agent
      - pos-live-agent
      - pos-admin-agent
    ports:
      - "7007:7007"
    command: ["proxy", "--agent-name", "host", "--host", "0.0.0.0", "--port", "7007"]
    restart: unless-stopped
