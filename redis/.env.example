# Optional but strongly recommended if this Redis is reachable outside localhost/VPC.
REDIS_PASSWORD=change-me

# Optional: additional redis-server args.
# Example:
#   REDIS_ARGS=--maxmemory 1gb --maxmemory-policy noeviction
# REDIS_ARGS=

